version: 2

models:
  - name: stg_stripe_payments
    description: Staged payments data from stripe schema.
    columns: 
      - name: payment_id
        description: The primary key for payments.
        tests:
          - unique
          - not_null